defaults:
  - default

dataset_name: QMUGSBin0QM9

basis_info:
  atomic_numbers: [1, 6, 7, 8, 9]

dataset_statistics:
  _target_: mldft.ml.preprocess.dataset_statistics.DatasetStatistics
  # Use the QMUGSBin0 statistics since they generalize better to large QMUGS molecules
  path: ${oc.env:DFT_DATA}/QMUGSBin0/dataset_statistics/dataset_statistics_labels_${data.transforms.name}_${data.datamodule.dataset_kwargs.energy_key}.zarr
